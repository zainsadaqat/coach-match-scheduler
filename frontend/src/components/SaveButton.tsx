import React from 'react';

const SaveButton = () => {
  return (
    <button
      type="submit"
      className="bg-[#052FFD] text-[#fff] font-semibold text-[14px] py-[8px] px-[2em] rounded"
    >
      Save
    </button>
  );
};

export default SaveButton;
